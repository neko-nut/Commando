#version 330

uniform mat4 view;
uniform mat4 proj;
uniform mat4 model;

in vec3 position;
in vec3 color;
in vec2 texCoord;

out vec3 ourColor;
out vec2 TexCoord;


void main()
{ 
	gl_Position = proj * view * model * vec4(position, 1.0f);
	ourColor = color;
    	TexCoord = texCoord;
}